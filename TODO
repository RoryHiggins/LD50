
debug-mode integration with imgui
- api build gate defaults to OD_BUILD_DEBUG

debug-mode integration with a backtrace lib (is boost backtrace isolated to boost core?)

debug-mode integration with tracy

Rendering
- MVP gl render pipeline - color only
	- enable calls for every render call, with glPushAttrib(GL_ENABLE_BIT)
- Texture creation and loading
- Render pipeline texture sampling
- Vertex array representation
- Vertex rendering
- Point to triangles converter
- Line to triangles converter
- Rect to triangles converter
- Primitive to triangles converter
- Triangle depth sorting


Rendering high-level
--------------------
odVertex
- sort_array()

odTexture

odTextureAtlas
- texture

- assign(*image, width, height) -> uv
- 

odDefaultRenderer
- render_context  // void* AKA SDL_GlContext
- vao
- program
- vert_shader
- frag_shader
- build_default(render_context)
- render(*vertices, *src_texture, *dest_texture, viewport)

Window
- sdl_gl_context
- get_render_context()
- ...
